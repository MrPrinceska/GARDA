---
title: Нумерация
order: 5
---

Плагин автоматически рассчитывает и заполняет параметр `ADSK_Позиция` по элементам активной спецификации.

## Где находится

Плагин расположен на вкладке `GARDA_ВИС`, панели `[указать панель]`.

## Как пользоваться

1. Откройте спецификацию, по которой необходимо выполнить нумерацию.

2. Запустите плагин `Нумерация ADSK_Позиция`.

3. Подтвердите запуск.

4. Дождитесь завершения обработки.

Обрабатываются только элементы, которые попадают в активную спецификацию с учётом её фильтров.

## Как формируется позиция

Элементы сортируются в фиксированном порядке:

1. `G_СО_Имя системы`;

2. `G_СО_Группирование`;

3. `ADSK_Наименование`;

4. `ADSK_Марка`.

После сортировки элементам последовательно присваиваются позиции:

`1`, `2`, `3`, `4`...

Если у нескольких элементов совпадают значения всех четырёх параметров, они получают одинаковую позицию.

### Вложенные элементы

Для вложенных семейств используется позиция родительского элемента.

Например:

-  родитель -- `15`;

-  первый вложенный элемент -- `15.1`;

-  второй -- `15.2`;

-  третий -- `15.3`.

Подпозиции вложенных элементов формируются по `ADSK_Наименование`.

Одинаковые вложенные элементы у родителей с одинаковой основной позицией получают одинаковую подпозицию.

## Особенности

-  Плагин работает только из активной спецификации.

-  Из настроек спецификации учитываются только её фильтры.

-  Сортировка самой спецификации на результат не влияет.

-  Вложенные элементы не получают отдельную основную позицию.

-  Существующие значения `ADSK_Позиция` перезаписываются.

**Важно**

Перед запуском убедитесь, что активна нужная спецификация и корректно заполнены параметры:

`G_СО_Имя системы`, `G_СО_Группирование`, `ADSK_Наименование`, `ADSK_Марка`.